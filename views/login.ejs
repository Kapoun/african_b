
<div class="container">
<h1>Log in</h1> 

<form method="POST" action="/login">
  <label for="text">Enter phone number</label>
  <input type="text" name="phone_number" placeholder="Phone Number" /> <br>
  <label for="password">Enter password</label>
  <input type="password" name="password" placeholder="Password" /> <br>
  <button type="submit">Login</button>
</form>

</div>



<style>

/* === Container Styling === */
.container {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  background: linear-gradient(to bottom right, #a1ffce, #faffd1); /* pastel gradient vibes */
  padding: 2rem;
  font-family: "Segoe UI", sans-serif;
}

/* === Heading === */
.container h1 {
  font-size: 2.5rem;
  margin-bottom: 1.5rem;
  color: #222;
}

/* === Form Styling === */
form {
  background-color: white;
  padding: 2rem 2.5rem;
  border-radius: 12px;
  box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
  display: flex;
  flex-direction: column;
  gap: 1rem;
  width: 100%;
  max-width: 400px;
}

/* === Label Styling === */
form label {
  font-weight: 600;
  color: #444;
}

/* === Input Styling === */



</style>